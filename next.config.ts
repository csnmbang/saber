import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // CLAUDE.md is the product spec, not a generated file. Keep next dev out of it.
  agentRules: false,
};

export default nextConfig;
