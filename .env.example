# Neon Postgres. Vercel > your project > Storage > Neon writes this for you;
# run `vercel env pull .env.local` to get it locally.
DATABASE_URL=

# Signs the cookie that lets an uploader reopen their own unpublished set.
# Any random string of 32+ characters: openssl rand -base64 32
SESSION_SECRET=
